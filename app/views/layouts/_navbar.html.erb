<nav class="navbar navbar-default navbar-fixed">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navigation-example-2">
        <span class="sr-only">Toggle navigation</span><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span>
      </button>
      <%= current_path %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-left">
        <% if notice %>
        <li class="alert alert-warning alert-dismissible" role="alert">
          <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <%= notice %>
        </li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li style="padding-top:19px;color:#6591CC">
            <i class="fa fa-user fa-lg"></i>
            @<%= current_user.user_name %> | 
            <%= current_user.role.role_name %> <%= current_user.outlet.outlet_name %>
          </li>
          
          <li class="dropdown" id="notifDropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" id="notifLink_<%= current_user.outlet_id %>">
              <span id="ntf_<%= current_user.outlet_id %>"><%= ntf %></span>
              <b class="caret"></b>
              <span class="notification" id="notifCounter_<%= current_user.outlet_id %>"><%= counterNotif %></span>
            </a>
            <ul class="dropdown-menu" id="notifList_<%= current_user.outlet_id %>">
              <%= render partial: 'notifications/index' %>
            </ul>
          </li>
          

          <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Daftar", new_user_registration_path %></li>
          <li><%= link_to "Masuk", new_user_session_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>